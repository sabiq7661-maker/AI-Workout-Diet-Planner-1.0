<!DOCTYPE html>
<html>
<head>
  <title>Your Fitness Plan</title>
  <style>
    body { font-family: Arial; background:
    .box { highest-width:850px; margin:auto; background:white; padding:20px; border-radius:12px; }
    .card { background:
    h2, h3 { text-align:center; }
    ul { line-height:1.8; }
    a { display:block; text-align:center; margin-top:20px; color:black; text-decoration:none; }
    .highlight { font-weight:bold; }
  </style>
</head>
<body>
  <div class="box">
    <h2>Personalized Plan for {{name}}</h2>

    <div class="card">
      <p><span class="highlight">Age:</span> {{age}} | <span class="highlight">Gender:</span> {{gender}}</p>
      <p><span class="highlight">BMI:</span> {{bmi}} ({{bmi_cat}})</p>
      <p><span class="highlight">BMR:</span> {{bmr}} kcal/day</p>
      <p><span class="highlight">TDEE:</span> {{tdee}} kcal/day</p>
      <p><span class="highlight">Goal:</span> {{goal_text}}</p>
      <p><span class="highlight">Daily Calories:</span> {{cal_target}} kcal/day</p>
      <p><span class="highlight">Protein Target:</span> {{prot_target}} g/day</p>
    </div>

    <div class="card">
      <h3> Workout Plan ({{days}} days/week - {{mode}})</h3>
      <ul>
        {% for w in workout %}
          <li>{{w}}</li>
        {% endfor %}
      </ul>

      <h4>Workout Tips</h4>
      <ul>
        {% for t in wtips %}
          <li>{{t}}</li>
        {% endfor %}
      </ul>
    </div>

    <div class="card">
      <h3> Diet Plan (Daily)</h3>
      <ul>
        {% for d in diet %}
          <li>{{d}}</li>
        {% endfor %}
      </ul>

      <h4>Diet Tips</h4>
      <ul>
        {% for t in dtips %}
          <li>{{t}}</li>
        {% endfor %}
      </ul>
    </div>

    <a href="/">⬅ Back to Home</a>
  </div>
</body>
</html>